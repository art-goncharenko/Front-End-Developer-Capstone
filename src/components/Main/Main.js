import "./Main.css";
import {Hero} from "../Hero/Hero";
import {Specials} from "../Specials/Specials";

export const Main = () => {
    return (
            <main>
                <Hero/>
                <Specials/>
            </main>
    );
}